import LoginWithPassword from '@/components/login/loginWithPassword';

const LoginPage = () => {
  return (
    <main>
      <div>
        <LoginWithPassword />
      </div>
    </main>
  );
};

export default LoginPage;
